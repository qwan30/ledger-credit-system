interface:
  display_name: "Verification Loop"
  short_description: "Build, test, lint, typecheck verification"
  brand_color: "#10B981"
  default_prompt: "Use $verification-loop to run the repository verification checklist before review."
policy:
  allow_implicit_invocation: true

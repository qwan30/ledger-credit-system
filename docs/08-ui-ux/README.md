# UI/UX Overview

The web workspace is present but intentionally minimal. It supports a small customer-facing flow and auth proxy routes rather than a complete operations console.

| Area | Current status |
|---|---|
| Framework | Next.js workspace under `apps/web` |
| Public page | Present |
| Login page | Present |
| Customer dashboard | Present |
| Transfer creation/detail pages | Present |
| Credit assessment creation/detail pages | Present |
| Ops/admin UI | Not present in current web workspace |

Use backend docs as the source of workflow truth when a UI screen is not implemented.
